version: "3"

services:
  accountdb:
    image: mongo:4.1.12
    restart: always
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_ROOT_USERNAME: mongoUser
      MONGO_INITDB_ROOT_PASSWORD: mongoPassword
    volumes:
      - /opt/data/db:/data/db
  txdb:
    image: mysql
    restart: always
    container_name: "txdb"
    environment:
      MYSQL_USER: user
      MYSQL_PASSWORD: manman
      MYSQL_DATABASE: transactions
      MYSQL_ROOT_PASSWORD: txdbpassword
    command: --default-authentication-plugin=mysql_native_password
    ports:
      - "3306:3306"
    volumes:
      - /opt/mysqldb:/var/lib/mysql
  account-service:
    build: ./src/account-service
    restart: always
    depends_on:
      - accountdb
    ports:
      - "9001:9001"
  transaction-service:
    build: ./src/transaction-service
    restart: always
    depends_on:
      - txdb
      - account-service
    ports:
      - "9002:9002"
  nginx:
    build: ./nginx
    restart: always
    ports:
      - "80:80"
    depends_on:
      - account-service
      - transaction-service